package tutorial

object Timer {
  def onePersecond(callback: () => Unit) {
    while (true) {callback(); Thread sleep 1000 }
  }
  
  def timeFlies() {
    println "time flies like an arrow..."
  } 
}